AHOS Windows owner - three commands toward PRE_SOAK (PAPER_ONLY)
================================================================
PR #31 (web API auth) + PR #32 (gate runner) are on main.
STATE B: do NOT db:migrate / db:push.
Do NOT invent OPERATOR_READY.

Option A - double-click (needs PR #33 merge, or cherry-pick AHOS_WINDOWS_OPS.bat):
  AHOS_WINDOWS_OPS.bat
  (pull → reconcile → preflight → starts npm run dev → waits :3000 → gate)

Option B - three commands on current main:
cd G:\robat\ahos
git pull
powershell -ExecutionPolicy Bypass -File .\scripts\windows_post_merge_reconcile.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\windows_preflight_ops.ps1

# Terminal A
npm run dev

# Terminal B (after Next is up)
powershell -ExecutionPolicy Bypass -File .\scripts\windows_run_operator_gate.ps1

Paste into Cursor:
  - BEGIN REPORT from reconcile (if shown)
  - reports\OWNER_PASTE_WINDOWS_GATE.txt  (preferred after gate)
  - or reports\LATEST_WINDOWS_GATE.txt + reports\operator_validation_report_windows_*.json
