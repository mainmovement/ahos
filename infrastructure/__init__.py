"""DEPRECATED PARALLEL SUBSYSTEM (PR #14 / non-canonical).

Not part of AHOS canonical architecture. Canonical configuration lives in
`config/paths.py`, `config/*.yaml`, and `.env.example`. Canonical HTTP
provider adapters live in `architecture/providers/`.
Do not import this package from the runtime.
"""
