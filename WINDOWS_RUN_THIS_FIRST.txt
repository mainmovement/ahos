AHOS Windows -- PRE_SOAK unlock (PAPER_ONLY)
==========================================
STATE B: NEVER run db:migrate / db:push
Do NOT invent OPERATOR_READY / PRE_SOAK without Windows OWNER_PASTE
Lane-A freeze: do not modify frozen Lane-A paths.

On main (merged): web-api auth (#31), G2 empty-gateway (#45), G2 validate (#46),
evidence autopush (#47), /api/chat 500 stack + DATABASE_URL sync (#48-#52).
Unlock tip: PR #53 (warm recovery + OPS sync fix).

--------------------------------------------------
ONE CLICK (preferred)
--------------------------------------------------
  cd /d G:\robat\ahos
  git pull origin main
  AHOS_PRE_SOAK_NOW.bat

  Bat: token + Docker diagnose + DATABASE_URL (probe-first), then AHOS_WINDOWS_OPS.bat
  On /api/chat warm fail: auto forensics + DATABASE_URL + ensure-pg + Next restart.
  Paste reports\OWNER_PASTE_WINDOWS_GATE.txt
  Or: AHOS_PUSH_EVIDENCE_NOW.bat (pushes existing paste to PR #54 / evidence branch)
  PRE_SOAK only if paste shows pre_soak_entry_ok=true (G1-G10).

--------------------------------------------------
OR: G2 first (if /api/chat still 500)
--------------------------------------------------
  AHOS_VALIDATE_G2_NOW.bat
  (on G2 PASS, chains into AHOS_PRE_SOAK_NOW.bat)

  Manual forensics if still 500:
    powershell -ExecutionPolicy Bypass -File .\scripts\windows_chat_500_forensics.ps1
    powershell -ExecutionPolicy Bypass -File .\scripts\windows_ensure_database_url.ps1
    powershell -ExecutionPolicy Bypass -File .\scripts\windows_restart_next_dev.ps1

Never invent READY. G11 Telegram is OWNER_ACTION for OPERATOR_READY only.
ahos_runtime_win unhealthy is OK for host Next G2.
