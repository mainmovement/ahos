AHOS Windows -- PRE_SOAK unlock (PAPER_ONLY)
==========================================
STATE B: NEVER run db:migrate / db:push
Do NOT invent OPERATOR_READY / PRE_SOAK without Windows OWNER_PASTE

On main now: through PR #43 (G2 gateway fill, Docker wait, G12 UTF-8,
reconcile dirty allow, unlock bat). Tip includes auth + ops harden path.

Latest Windows-attested gate before #43 (220318):
  PASS: G1, G3-G10
  BLOCKED: G2 empty AHOS_GATEWAY_URL /api/chat 500 without Docker
  OWNER_ACTION: G11 Telegram (READY only, not PRE_SOAK)
  pre_soak_entry_ok=false

--------------------------------------------------
A) Pull latest main
--------------------------------------------------
  cd /d G:\robat\ahos
  git checkout main
  git pull origin main

B) Start Docker Desktop
  - Wait until GREEN / Linux Engine running
  - Confirm:  docker ps

C) One-shot ops
  Double-click:  AHOS_WINDOWS_OPS.bat

  Bat: wait for Docker -> ensure ahos_postgres_win (no migrate) ->
  reconcile -> ensure token + AHOS_GATEWAY_URL=http://127.0.0.1:3000/api/chat ->
  preflight -> restart Next -> warm /api/chat -> seed SQLite if needed -> gate.

D) Paste evidence into Cursor (REQUIRED)
  reports\OWNER_PASTE_WINDOWS_GATE.txt

PRE_SOAK: only when paste shows pre_soak_entry_ok=true (G1-G10 PASS).
OPERATOR_READY: only with G11 Telegram E2E PASS -- never invent.
Postgres ahos_* rows do NOT satisfy G4/G5/G8/G9 (local SQLite census does).
