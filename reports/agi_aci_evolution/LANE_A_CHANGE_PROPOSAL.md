# LANE_A_CHANGE_PROPOSAL

**Status:** NONE THIS PASS

No Lane-A change is proposed. Cognitive evolution is `B_ONLY` (`architecture/cognitive/evolution_gate.py`).

If a future change would affect:

- `discovery/**` or `paper_trading/**`
- Lane-A scoring semantics
- production observation semantics
- active calibration
- soak T0 / daemon lifecycle

then stop and fill:

```text
reason:
affected contract:
evidence:
risk:
migration strategy:
freeze implications:
required human decision:
```

Do not implement that change on the soak-era branch without human approval.
