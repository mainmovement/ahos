# Historical wave snap manifests (archived)

These `ahos_snap_w*_after.txt` files are **historical C-class evidence** from early waves.
They lived at the repository root and cluttered the working tree.

**Current Lane-A integrity baseline (authoritative):** `config/lane_a_freeze.sha256`  
Regenerate / verify with: `python scripts/freeze_lane_a.py`

Do not treat these archived snaps as the live freeze surface.
