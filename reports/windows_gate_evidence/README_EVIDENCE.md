# Windows gate evidence sink

Populated by `scripts/windows_push_gate_evidence.ps1` after the laptop gate runs.

**Not** an OPERATOR_READY claim. STATE B: no `db:migrate` / `db:push`.

Agents: fetch `origin/cursor/windows-gate-evidence-4bde` and read files here.
