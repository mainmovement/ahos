# OSS research notes (archived from repo root)

These `OSS_*.md` files are research / mapping artifacts from the Top-20 OSS harvest waves.
They are **not** the AHOS runtime AI router and **not** Cursor Cloud Agent policy.

Authoritative runtime AI docs remain under `docs/architecture/` and `config/ai_*.yaml`.
Operational honesty for open gaps: `AHOS_GAP_REGISTER.md` (repo root).
