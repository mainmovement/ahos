# AHOS Drizzle migrations (PostgreSQL)
#
# Canonical schema: ../schema.ts (19 ahos_* tables only).
# Generated by: npm run db:generate
# Applied only when owner authorizes: npm run db:migrate
#
# NEVER drizzle-kit push against a DB that already holds unrelated public tables
# without reviewing the SQL. Existing n8n / legacy tables must remain untouched.
