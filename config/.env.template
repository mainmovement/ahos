# AHOS environment template — copy to .env and fill. NEVER commit .env, NEVER log values.
# ---- Telegram (Agent-04/05) ----
# Old Sun_sniper token is EXPOSED (was pasted in chat) — revoke via @BotFather FIRST, then paste NEW token.
TELEGRAM_BOT_TOKEN=__NEW_TOKEN_AFTER_REVOCATION__
TELEGRAM_ADMIN_CHAT_ID=__YOUR_NUMERIC_CHAT_ID__   # run /start once, read from getUpdates
# ---- Exchange (trade-only key, withdrawal DISABLED, IP whitelist if supported) ----
EXCHANGE=lbank            # lbank | bybit (config-swappable, no rewrite)
EXCHANGE_API_KEY=
EXCHANGE_API_SECRET=
# ---- Database ----
POSTGRES_DB=ahos
POSTGRES_USER=ahos_app
POSTGRES_PASSWORD=__STRONG_RANDOM__
# ---- n8n ----
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=__STRONG_RANDOM__
N8N_ENCRYPTION_KEY=__openssl_rand_hex_24__
N8N_HOST=127.0.0.1
N8N_PROTOCOL=https
N8N_WEBHOOK_URL=https://__your_tunnel_domain__/n8n/
# ---- System mode ----
AHOS_MODE=PAPER           # PAPER until ALL gates pass; LIVE only with Agent-10 + Human Gate
AHOS_CAPITAL_USDT=10      # micro-capital mode -> leverage hard-capped at 2x
