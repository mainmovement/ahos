# AHOS — راه‌اندازی سریع

> از صفر تا ربات فعال. کپی کنید، پیست کنید، اجرا شود.

---

## پیش‌نیاز

فقط **Python 3.11 یا بالاتر**. هیچ چیز دیگری لازم نیست — نه سرور، نه دیتابیس
جداگانه، نه کلید API پولی.

بررسی نسخه:

```bash
python --version
```

---

## نصب (۵ دستور)

### لینوکس / مک

```bash
git clone <repo-url> ahos && cd ahos
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
python scripts/init_databases.py --with-guards
cp .env.example .env
```

### ویندوز (PowerShell؛ مسیر پیشنهادی)

```powershell
git clone <repo-url> ahos; cd ahos
Set-ExecutionPolicy -Scope Process Bypass
.\install_windows.ps1
```

نصاب نسخهٔ Python 3.11+ را واقعاً بررسی می‌کند، `.venv` می‌سازد، UTF-8 و
SQLite را آماده می‌کند و تست‌های آفلاین اولیه را اجرا می‌کند.

---

## پیکربندی

فایل `.env` را باز کنید. برای اجرای **ربات تلگرام** فقط این مقدار اجباری است؛
موتور قطعی و حالت کنسول بدون آن اجرا می‌شوند:

```ini
TELEGRAM_BOT_TOKEN=123456789:AAF...
```

توکن را از [@BotFather](https://t.me/BotFather) در تلگرام بگیرید: دستور `/newbot`.

### دو تنظیم که قویاً توصیه می‌شود

```ini
# فقط خودتان بتوانید با ربات کار کنید.
# آیدی عددی خود را از @userinfobot بگیرید.
TELEGRAM_ALLOWED_CHAT_IDS=123456789

# اگر در ایران هستید: تلگرام فیلتر است.
# آدرس تونل محلی خودتان را بگذارید.
ALL_PROXY=socks5://127.0.0.1:10808
```

بقیه مقادیر `.env` **همه اختیاری‌اند**. منطق قطعی بدون کلید پولی اجرا می‌شود؛
اما نبود شبکه، محدودیت RPC یا فیلترینگ می‌تواند دادهٔ زنده را `UNKNOWN` کند و
نباید با «ظرفیت کامل» یا دادهٔ موجود اشتباه گرفته شود.

---

## اجرا

```bash
python run_bot.py --preflight    # بررسی: آیا همه‌چیز آماده است؟
python run_bot.py                # اجرای ربات
```

`--preflight` قبل از اجرا هر مشکلی را با راه‌حلش گزارش می‌دهد: توکن، پروکسی،
دیتابیس، اتصال. اگر چیزی سبز نبود، دقیقاً می‌گوید چه کار کنید.

### بدون تلگرام (وقتی تونل قطع است)

```bash
python run_bot.py --console
```

همان مغز، همان تحلیل‌ها، بدون هیچ نیاز به شبکه. اگر فیلترینگ اجازه نداد به تلگرام
وصل شوید، سامانه بی‌استفاده نمی‌شود.

### جمع‌آوری داده

ربات از روی داده‌های ذخیره‌شده جواب می‌دهد. برای پر کردن پایگاه داده:

```bash
# یک چرخهٔ امتیازدهی + مشاهده (شواهد واقعی اپراتور)
python -m architecture.runtime --single-cycle --observation-cycle --evidence-source local

# اجرای مداوم
python -m architecture.runtime --daemon --interval-sec 60 \
  --observation-cycle --evidence-source local
```

اجرای `architecture.runtime` بدون `--observation-cycle` عمداً فقط پایپ‌لاین
امتیازدهی را اجرا می‌کند و برای جمع‌آوری نتیجه/کالیبراسیون کافی نیست.

---

## چه چیزی می‌توانید بپرسید

با ربات مثل یک آدم حرف بزنید. فارسی، آزاد، بدون دستور خاص:

| می‌پرسید | چه کار می‌کند |
|---|---|
| `سلام` | معرفی و راهنما |
| `امروز چه خبر؟` | خلاصه اخبار کریپتو با تحلیل فضا |
| `چی بخرم؟` | تحلیل فرصت‌های فعلی، با دلیل کامل |
| `بهترین فرصت های امروز` | رتبه‌بندی توکن‌های رصدشده |
| `این توکن رو بررسی کن [آدرس]` | تحلیل کامل یک توکن |
| `میتونم بفروشمش؟` | آیا پول واقعاً بیرون می‌آید؟ |
| `نهنگ ها چیکار میکنن؟` | توزیع مالکیت و تمرکز |
| `این وایرال شده؟` | سنجش توجه بازار از روی زنجیره |
| `نظر هوش مصنوعی ها چیه؟` | جمع‌بندی شورای مدل‌ها |
| `۵ میلیون تومان خریدم` | ثبت پوزیشن در دفتر کاغذی |
| `چند درصد سود دارم؟` | وضعیت پوزیشن‌های شما |
| `کی بفروشم؟` | تحلیل زمان خروج |
| `اشتباهاتت رو مرور کن` | بازبینی انتخاب‌های گذشته |
| `وضعیت سیستم چطوره؟` | سلامت کامل سامانه |

---

## هوش مصنوعی (کاملاً اختیاری)

بدون هیچ کلیدی، شورا وضعیت `OFFLINE` می‌دهد و **موتور قطعی ریاضی بدون وقفه کار
می‌کند**. نظر مدل‌ها هیچ‌وقت نمی‌تواند وتوی موتور قطعی را لغو کند — پس نبودشان
چیزی را نمی‌شکند.

اگر خواستید فعالشان کنید، بهترین گزینه برای ایران **Ollama** است: محلی، رایگان،
غیرقابل فیلتر.

```bash
# از ollama.com نصب کنید، سپس:
ollama pull qwen2.5:7b-instruct
```

مدل‌های ابری رایگان (Groq، Gemini، OpenRouter، GitHub Models) هم پشتیبانی
می‌شوند؛ کلیدشان را در `.env` بگذارید.

---

## عیب‌یابی

| نشانه | علت و راه‌حل |
|---|---|
| `❌ تلگرام پاسخ نداد` | تونل روشن نیست یا `ALL_PROXY` اشتباه است |
| `❌ PySocks نصب نیست` | `pip install PySocks` |
| `❌ پایگاه داده ساخته نشده` | `python scripts/init_databases.py --with-guards` |
| «هیچ توکنی ثبت نشده» | هنوز داده جمع نشده: `python -m architecture.runtime --single-cycle` |
| اخبار همیشه در دسترس نیست | منابع خبری فیلترند؛ `ALL_PROXY` را تنظیم کنید |
| شورا همیشه OFFLINE است | طبیعی است بدون کلید. Ollama را نصب کنید یا نادیده بگیرید |

اجرای تست‌ها برای اطمینان از سلامت نصب:

```bash
python -m pytest tests/ -q
```

---

## چند نکته که باید بدانید

**این سامانه معامله نمی‌کند.** هیچ مسیر کدی برای خرید و فروش واقعی وجود ندارد.
ابزار تحلیل و پشتیبانی تصمیم است. هر پاسخ با «تصمیم نهایی با کاربر است.» تمام
می‌شود، و این یک تعارف نیست — قانون معماری است.

**«نمی‌دانم» یک پاسخ معتبر است.** وقتی داده‌ای در دسترس نیست، سامانه
`UNKNOWN` می‌گوید و علتش را توضیح می‌دهد. هیچ‌وقت نبودِ داده را «امن» تفسیر
نمی‌کند و هیچ‌وقت عدد جعلی نمی‌سازد.

**مهم‌ترین سؤال، «قیمت بالا می‌رود؟» نیست؛ «پول بیرون می‌آید؟» است.** سامانه
ارزش واقعیِ قابل برداشت را حساب می‌کند — با احتساب لغزش، کارمزد، مالیات فروش و
عمق استخر. توکنی که روی نمودار ۵ برابر شده اما استخرش ۹۰۰ دلار است، «تله»
علامت می‌خورد، نه «فرصت».
