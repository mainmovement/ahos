AHOS -- RUN ME ON WINDOWS (PAPER_ONLY)
======================================
STATE B: NEVER db:migrate / db:push
Do NOT invent PRE_SOAK / OPERATOR_READY without OWNER_PASTE

Last evidence (20260828_220318): G2 BLOCKED on empty AHOS_GATEWAY_URL;
G3-G10 already PASS. Empty-gateway fix is already on main (#45).

Please merge unlock PR #58. Keep #56 OPEN.

WARNING: older tips served LF-only .bat via raw GitHub (cmd.exe breaks).
This tip stores .bat as CRLF in git (`*.bat -text`). Prefer PowerShell entry:

--------------------------------------------------
A) CRLF-safe one-liner (PREFERRED — pure PS surgical G2+gate)
--------------------------------------------------
  cd /d G:\robat\ahos
  powershell -NoProfile -ExecutionPolicy Bypass -Command "iex (iwr -useb https://raw.githubusercontent.com/mainmovement/ahos/cursor/windows-main-evidence-push-4bde/AHOS_RUN_TIP.ps1).Content"

  (defaults to windows_fix_g2_empty_and_gate.ps1 — correct for last paste)

--------------------------------------------------
B) curl .bat also OK on this tip (CRLF in blob)
--------------------------------------------------
  curl.exe -L -o AHOS_MAIN_FIRST.bat https://raw.githubusercontent.com/mainmovement/ahos/cursor/windows-main-evidence-push-4bde/AHOS_MAIN_FIRST.bat
  AHOS_MAIN_FIRST.bat

--------------------------------------------------
C) If A/B fail / chat cold — FIX_G2 mode
--------------------------------------------------
  powershell -NoProfile -ExecutionPolicy Bypass -File .\AHOS_RUN_TIP.ps1 -Mode fix_g2

--------------------------------------------------
D) After git pull of tip / merge of #58
--------------------------------------------------
  git fetch origin cursor/windows-main-evidence-push-4bde
  git checkout origin/cursor/windows-main-evidence-push-4bde -- AHOS_MAIN_FIRST.bat
  AHOS_MAIN_FIRST.bat

PRE_SOAK only if pre_soak_entry_ok=true (G1-G10). Never invent READY.
Merge #58. Leave paste-sink PR #56 OPEN.
